import 'package:flutter/material.dart';

class AlamlarPage extends StatelessWidget {
  const AlamlarPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Alamlar Sayfası",
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
